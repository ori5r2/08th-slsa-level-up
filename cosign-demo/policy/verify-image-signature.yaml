# verify-image-signature.yaml

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: verify-signed-images
spec:
  validationFailureAction: Enforce
  background: false
  rules:
    - name: check-signature
      match:
        resources:
          kinds:
            - Pod
      verifyImages:
        - imageReferences:
            - "ori5r2/slsa*"
            - "ghcr.io/ori5r2/slsa*"
          required: true
          verifyDigest: true
          attestors:
            - entries:
                - keyless:
                    rekor:
                      url: https://rekor.sigstore.dev
                    issuer: "https://token.actions.githubusercontent.com"
                    subject: "https://github.com/ori5r2/08th-slsa-level-up/.github/workflows/build-sign-cosign-demo.yaml@refs/heads/main"
